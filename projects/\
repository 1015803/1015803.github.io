<!DOCTYPE html>
<html lang="en">
	<head>
		<title>This is my portrait</title>
		<style>
			canvas {
				border: black 1px solid;
				}
		</style>
	</head>
	<body>
		<h1>This is canvas</h1>
		<canvas id="canvas" width="1000" height="1000">
		</canvas>
		<script>
			//functions


			function background(sx, sy, color) {
				ctx.fillStyle = color;
				ctx.fillRect(0, 0, sx, sy,);
			}

			function triangle(x1, y1, x2, y2, x3, y3, color) {// (point 1 x, point 1 y, point 2 x, point 2 y, point3 x, point 3 y, color of triangle)
				ctx.fillStyle = color;
				ctx.beginPath();
 	                        ctx.moveTo(x1, y1);//point 1
       		                ctx.lineTo(x2, y2);//point 2
 	                        ctx.lineTo(x3, y3);//point 3
				ctx.fill();
			}

			function oval(x1, y1, height, width, rotation, portion, color) {// (center x, center y, width, height, rotation in degrees, states 1/2 or whole, color)
				ctx.fillStyle = color; 
				ctx.beginPath();
				rotation = (rotation * Math.PI / 180.0);//turns degrees into radians
				if (portion = "half") {
					ctx.ellipse(x1, y1, height, width, rotation, 0, 1*Math.PI);//dont f, with 0 no clue what it does
				}
				if (portion = "whole") {
					ctx.ellipse(x1, y1, height, width, rotation, 0, 2*Math.PI);//dont change 0!
				}
				ctx.fill();
			}

			function circle(x1, y1, r, rotation, portion, color) {// (center x, center y, radius, rotation in degrees, states 1/2 or whole, color)
				ctx.fillStyle = color;                          
                                ctx.beginPath();                                
                                rotation = (rotation * Math.PI / 180.0);//turns degrees into radians
				if (portion = "half") {                         
					ctx.ellipse(x1, y1, r, r, rotation, 0, 1*Math.PI);//dont f, with 0 no clue what it do    es
                                }                                               
                                if (portion = "whole") {                        
                                        ctx.ellipse(x1, y1, r, r, rotation, 0, 2*Math.PI);//dont change 0!
                                }                                               
                                ctx.fill();
			}

			function square (x, y, size, color) {
				ctx.fillStyle = color;
				ctx.fillRect(x, y, size, size);
			}

			function rectangle (x1, y1, x2, y2, color) {
				ctx.fillStyle = color;
				ctx.fillRect(x1, y1, x2, y2);
			}
			const canvas = document.getElementById("canvas");
			const ctx = canvas.getContext("2d");

			//examples
			//background(800, 800, "grey");
			//square(0, 0, 100, "blue");
			//rectangle(100, 100, 200, 300, "yellow");
			//square(0, 0, 100, "blue");
			//circle(500, 500, 100, 0, "whole", "orange");
			//oval (300, 200, 100, 50, 90, "whole", "blue");
			//triangle(700, 700, 780, 800, 620, 800, "red");

background(1000, 1000, "lightgray");


// ================= HEAD =================
oval(500, 360, 135, 185, 0, "whole", "peach");
oval(500, 385, 125, 165, 0, "whole", "tan");

// jaw
triangle(380, 430, 500, 520, 620, 430, "tan");


// ================= HAIR =================
oval(500, 250, 155, 120, 0, "whole", "saddlebrown");
oval(420, 300, 70, 85, -20, "whole", "saddlebrown");
oval(580, 300, 70, 85, 20, "whole", "saddlebrown");
triangle(420, 330, 500, 200, 580, 330, "saddlebrown");


// ================= EARS =================
oval(350, 380, 28, 55, 0, "whole", "tan");
oval(650, 380, 28, 55, 0, "whole", "tan");


// ================= EYES =================
oval(445, 345, 34, 22, 0, "whole", "white");
oval(555, 345, 34, 22, 0, "whole", "white");

circle(445, 345, 13, 0, "whole", "black");
circle(555, 345, 13, 0, "whole", "black");

circle(452, 338, 4, 0, "whole", "white");
circle(562, 338, 4, 0, "whole", "white");


// ================= EYEBROWS =================
rectangle(395, 305, 95, 14, "brown");
rectangle(510, 305, 95, 14, "brown");


// ================= NOSE =================
triangle(500, 355, 490, 415, 510, 415, "tan");
oval(500, 420, 18, 8, 0, "whole", "peru");


// ================= MOUTH =================
oval(500, 465, 45, 20, 0, "whole", "rosybrown");
rectangle(455, 460, 90, 8, "indianred");


// ================= NECK =================
rectangle(455, 520, 90, 110, "tan");


// big shirt block
square(320, 590, 360, "white");

// left arm block
square(160, 640, 160, "peach");

// right arm block
square(680, 640, 160, "peach");



		</script>
	</body>
</html>
